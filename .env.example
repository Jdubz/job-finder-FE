# Environment Variables Example
# Copy this file to .env.development, .env.staging, or .env.production
# and fill in the values

# Firebase Configuration
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-auth-domain
VITE_FIREBASE_PROJECT_ID=job-finder-prod
VITE_FIREBASE_STORAGE_BUCKET=your-storage-bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# Backend API Configuration
# Note: In production, the baseUrl is auto-configured based on environment
# Development uses emulators or VITE_API_BASE_URL if VITE_USE_EMULATORS=false
VITE_API_BASE_URL=https://us-central1-job-finder-prod.cloudfunctions.net

# Development Only - Set to 'true' to use Firebase emulators
VITE_USE_EMULATORS=false

# Firestore Database ID
VITE_FIRESTORE_DATABASE_ID=(default)

# Emulator Configuration (for development only)
VITE_EMULATOR_HOST=localhost

# Build metadata
VITE_ENVIRONMENT=production

# Analytics
VITE_ENABLE_ANALYTICS=false

# Legacy environment variables (deprecated - kept for backward compatibility)
# These are no longer required as the API config auto-generates URLs
VITE_GENERATOR_API_URL=https://us-central1-job-finder-prod.cloudfunctions.net/manageGenerator
VITE_CONTENT_ITEMS_API_URL=https://us-central1-job-finder-prod.cloudfunctions.net/manageContentItems
VITE_JOB_QUEUE_API_URL=https://us-central1-job-finder-prod.cloudfunctions.net/manageJobQueue
VITE_CONTACT_FUNCTION_URL=https://us-central1-job-finder-prod.cloudfunctions.net/handleContactForm
